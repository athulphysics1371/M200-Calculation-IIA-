You may download the CSV files and upload them to the required folder before running the Python code. After that, download the Python script and execute it, 
ensuring that all the required libraries mentioned in the script are already installed on your Python environment.
